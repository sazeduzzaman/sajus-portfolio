import ProjectPage from '@/components/ProjectPage/ProjectPage';
import React from 'react';

const page = () => {
    return (
        <div>
            <ProjectPage/>
        </div>
    );
};

export default page;